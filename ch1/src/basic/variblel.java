package basic;

public class variblel {
    public static void main(String[] args) {
        // 변수 선언 후 값을 할당
        int num = 10;

        int sum = num + 20;
        // + :연결
        System.out.println("num 변수 값" + num);
        System.out.println("num + 20 =" + sum);
        System.out.printf(format:"num 변수 값: %d\n",num);
        // num 변수 값 : 10age=25,maxSpeed=10

        int age = 25, maxSpeed = 10;
        System.out.println("age=" + age + ", maxSpeed=" + maxSpeed);
        System.out.printf(format:"age = %d"maxSpeed = %d ?num);

        num = 35;
        // 상수 선언과 할당
        final int MAX_SPEED = 20;
        System.out.println(MAX_SPEED);
        // MAX SPEED = 30;
    }
}
